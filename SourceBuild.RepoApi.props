<?xml version="1.0" encoding="utf-8"?>
<Project ToolsVersion="14.0" DefaultTargets="Build" xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
  <PropertyGroup>
    <!-- Plug in Repo API arguments to the properties that control BuildTools. -->
    <PackageOutputPath Condition="'$(DotNet_OutputBlobFeedDir)' != ''">$(DotNet_OutputBlobFeedDir)packages/</PackageOutputPath>
  </PropertyGroup>
</Project>
